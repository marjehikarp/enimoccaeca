// JavaScript example with multiple mappings
const operations = {
    enqueue: enqueue,
    dequeue: dequeue,
    peek: peek,
};

// Call the 'enqueue' function using the object
operations.enqueue(item);
